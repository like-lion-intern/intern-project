# 강의 텍스트 전처리 (Version 1) 요약

## 1. 개요
* **목표:** NLP 분석(요약, 키워드 추출 등)을 원활하게 수행하기 위해 강의 트랜스크립트(txt) 파일의 노이즈를 제거하고 메인 텍스트만 추출합니다.
* **주요 작업 내용:** EDA(탐색적 데이터 분석)를 통해 화자 분포를 파악한 뒤, 메인 화자의 발화만 병합하고 노이즈(보조/잡음)를 제거하는 1차 전처리 스크립트를 작성 및 적용했습니다.

## 2. 전처리 파이프라인 (3-Stage Filtering)
1. **메인 화자 발화 병합 (Main Speaker Merging)**
   - 전체 발화량 대비 특정 비율(기본 5%) 이상을 차지하는 메인 화자를 식별합니다.
   - 다수의 화자가 기준을 넘는 경우 복수의 메인 화자로 간주하여 발화를 순서대로 포함합니다.
2. **노이즈 화자 제거 (Noise Speaker Filtering)**
   - 기준치 미만의 발화량을 가진 화자(예: 보조 강사, 짧은 대답, 시스템 메시지 등)들의 발화는 노이즈로 간주하여 필터링합니다.
3. **IT 전문 용어 교정 (Technical Term Replacement)**  *(추가됨)*
   - 음성 인식(STT) 과정에서 잘못 전사된 프로그래밍 및 IT 전문 용어를 올바른 영문/표준 명칭으로 교정합니다. (예: `마이에스q엘`, `마S큐L` -> `MySQL`)

## 3. 스크립트 상세 구현
### 3.1 1차 전처리 (`preprocess_text.py`)
* **타임스탬프 제거:** `^<\d{2}:\d{2}:\d{2}>\s*` 정규식을 사용하여 라인 시작 부분의 타임스탬프를 제거했습니다.
* **화자 ID 분리 및 제거:** `^\[?([a-f0-9]{8})\]?:?\s*(.*)` 정규식을 통해 8자리 헥사코드(화자 ID)를 식별하고, 결과 텍스트에는 화자 태그를 포함하지 않도록 정제했습니다.
* **텍스트 병합 방식:** 정제된 각 발화 문장은 원본의 문맥 흐름을 유지할 수 있도록 줄바꿈(`\n`)을 기준으로 자연스럽게 병합되었습니다.
* **저장 위치:** `강의 스크립트_1차_전처리완료` 디렉토리

### 3.2 2차 전처리: 용어 교정 (`replace_terms.py`)
* **다중 오타 매핑 딕셔너리:** 튜플 형식(`올바른 용어: (오타1, 오타2, ...)`)을 사용하여, 하나의 올바른 용어에 여러 가지 오인식 사례(오타)를 손쉽게 매핑할 수 있도록 구현했습니다.
* **카운팅 및 검증:** 각 파일별로 진행된 교정 건수를 카운트(`str.count()`)하고 터미널에 상세 내역(`wrong_term -> correct_term (N회)`)을 리포트하도록 하여 교정 품질을 추적할 수 있도록 했습니다.
* **저장 위치:** `강의 스크립트_2차_용어교정완료` 디렉토리

## 4. 결과 및 기대 효과
* 총 15개 파일에 대해 노이즈 화자 제거 및 388건 이상의 주요 IT 용어 교정이 완료되었습니다.
* 전처리된 파일들은 발화자나 시간표기가 삭제되었고 전문 용어가 통일된 형태의 텍스트로 보존되어 있습니다. 
* 이를 통해 형태소 분석기, LLM(대규모 언어 모델) 요약, 혹은 TF-IDF 기반 핵심 키워드 추출 시 도메인 주요 키워드(예: `Java IO`, `MySQL`, `class`)가 소실되거나 분절되는 것을 방지하여, 후속 NLP 분석의 정확도를 크게 향상시킬 수 있습니다.
